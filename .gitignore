# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfplan

# Keep the lockfile tracked for deterministic builds
# .terraform.lock.hcl
# Backend configs (keep real values local)
backend/*.hcl
!backend/*.example.hcl
